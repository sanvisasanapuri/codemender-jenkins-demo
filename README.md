# codemender-jenkins-demo
- Created to integrate codemender with jenkins CI and invoke during github pull request trigger

- Configured Jenkins CI to trigger exclusively on pull request creation and subsequent commits. Additionally, enforced branch protection to require pull requests and prevent direct commits to the repository.

